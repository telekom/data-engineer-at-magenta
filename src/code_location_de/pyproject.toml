[project]
name = "code-location-de"
version = "1.0.0"
requires-python = "==3.12.*"
description = "Dagster code location for the Data Engineer challenge"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["code_location_de"]
